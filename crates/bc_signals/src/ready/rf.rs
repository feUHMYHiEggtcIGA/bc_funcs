use linfa_trees::{
    RandomForest,
    RandomForestParams,
}

use bc_utils_lg::types::ml::TRAIN_POOL;


fn rf(
    model: RandomForest,
    params: RandomForestParams,
    train_pool: TRAIN_POOL,
) -> (f64, f64)
{
    
}
